日期：4.15

今日目标：
    启动 STM32F401 → STM32H743 平台迁移工作
    在 H7 平台上复现阶段二（PC ↔ MCU 串口闭环）
    对原有代码进行模块化重构，避免 main.c 继续膨胀

今日完成工作
    H7工程基础搭建与外设被指
        使用STM32CubeIDE完成基础工程创建
        配置USART1(115200, 8N1, TX/RX)
        配置GPIO(PB0/PB1)用于LED输出
        解决H7开发板供电问题（需手动上电）

        结论：
            H7最小系统(时钟+GPIO+UART)运行正常
            ST-LINK调试链路稳定

    UART服务模块迁移与重构
        基于F401阶段二代买，完成以下工作：
            提取串口逻辑为独立模块uart_service
            实现核心功能：
                周期发送REQ
                非阻塞单字节接收
                行缓存拼接
                基于strstr的YES/NO判定
            同时修复关键问题：
                忘记调用 UART_Service_Init() 导致模块未生效
                编译错误（变量未声明）通过补全模块结构解决
            结果：
                H7 → 周期发送 REQ（约2s）
            串口助手成功接收到数据，证明：
                UART发送链路完全打通
    
    PC端AI响应链路恢复
        切换至正确Python虚拟环境(解决 ultralytics 缺失问题)
        成功运行 pc_uart_responder.py
        PC端完成：
            REQ接收
            YOLO推理
            RESULT返回
        日志表现：
            [PC] RX: REQ
            [PC] TX: RESULT label=YES conf=0.xx
        说明：
            MCU → PC → AI → MCU 通信链路恢复成功
    
    LED驱动模块独立封装（关键化结构）
        完成从main.c中剥离硬件逻辑：
            新增模块：
                bsp_led.c / bsp_led.h
            功能：
                红灯（PB1）/ 绿灯（PB0）控制
                状态接口： 
                    LED_Show_Yes()
                    LED_Show_No()
            设计原则：
                低电平点亮(与硬件一致)
                状态互斥(避免双灯同时亮)

    通信模块与硬件模块解耦
        在 uart_service 中直接调用 LED 接口：
            UART解析 → LED状态更新
        实现结构：
            main
            ↓
            uart_service
            ↓
            bsp_led
        main.c仅负责：
            初始化
            调度

    重新在H7上完成闭环验证
        最终系统行为：
            MCU → REQ → PC
            PC → RESULT → MCU
            MCU → LED状态变化
        具体表现：
            YES → 红灯亮
            NO → 绿灯亮
            随测试图片轮询动态变化
        结论：
            H7平台阶段二闭环迁移成功

今日关键问题与解决：
    问题1：ST-LINK无法连接
        报错：Unable to get core ID
        原因：开发板未上电
        解决：手动供电
    
    问题2：串口无输出
        原因：未调用 UART_Service_Init()
        现象：主循环运行但UART无行为
        解决：补充初始化调用

    问题3：Python环境错误
        报错：No module named ultralytics
        原因：使用错误解释器
        解决：切换至AI虚拟环境

架构变化总结
    由原始结构：
        main.c（全部逻辑）
    升级为：
        main（调度）
        + uart_service（通信）
        + bsp_led（硬件抽象）
    实现：
        模块解耦
        可维护性提升
        为后续扩展（摄像头/帧协议）打基础

当前系统状态
    | 模块      | 状态   |
    | ------- | ---- |
    | UART通信  | ✅ 稳定 |
    | PC AI推理 | ✅ 稳定 |
    | MCU解析逻辑 | ✅ 正常 |
    | LED反馈   | ✅ 正常 |
    | 系统闭环    | ✅ 完整 |

下一步计划
    进入第四阶段：
        帧结构通信复现：
            MCU → PC：图像数据帧发送
            PC → MCU：RESULT 帧返回
            替代当前字符串协议（REQ / RESULT）
        目标：
            “文本协议 → 帧协议 → 图像数据流”

总结：
    今天完成了 STM32F401 到 STM32H743 平台的迁移，并在模块化重构基础上成功复现并验证了 PC–MCU 串口通信闭环，同时实现了 AI 推理结果驱动的物理 LED 状态反馈，为后续帧结构通信与图像传输奠定了稳定基础。

