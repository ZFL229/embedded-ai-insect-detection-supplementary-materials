日期：3.27
今日主要工作
    1.通信协议结构梳理和扩展设计
        明确当前通信系统采用“分层式协议结构”：
            底层：frame_codec（负责流逝切帧与CRC校验）
            上层：基于payload扩展的图像分片协议
        决定不修改底层结构，通过payload中引入：
            CHUNK_SEQ
            CHUNK_TOTAL
        实现图像分片语义
        结论：
            协议升级采用“扩展而非重构”的方式，保持底层稳定性

    2.图像分片发送机制实现
        将发送端逻辑由：
            “整张图片单帧发送”
                改为
            “整图切片-多帧发送”
        实现功能：
            图像按固定大小（256B）分片
            每片封装为TYPE_IMAGE_CHUCK帧
            按序逐帧发送
        实现编号分离：
            frame_seq：传输层帧序号
            ima_id：应用层图像编号

    3.发送端运行验证
        成功建立TCP连接
        成功读取本地测试图片
        成功完整分片发送（示例：5073 Bytes-20 chunks）
        验证帧长度与协议结构一致：
            满片：277 Bytes
            尾片：符合剩余数据计算
        
        结论：
            发送端分片逻辑与协议设计完全一致，运行稳定

    4.系统行为分析（无MCU情况下）
        运行过程中接收端出现串口相关错误
        分析确认
            错误来源为未连接MCU（串口不存在）
            属于系统预测行为，而非通信异常

        结论：
            当前阶段问题不在发送端，而在下游依赖（MCU未接入）

今日关键结论
    图像分片发送链路已验证完成
    协议扩展方案正确（payload层实现）
    当前系统可稳定运行至“接收端入口”阶段
    接下来应将验证重点转移至接收端重组逻辑

下一步计划
    新建独立验证文件，实现：
        分片接收-重组-保存图像
    暂时移除MCU依赖，单独验证通信链路正确性